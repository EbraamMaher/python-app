pipeline{

    agent {label 'agent'}
    
    stages{
        stage('Build image'){
            steps{
               
                sh "docker image ls"
                sh "docker ps"
          
            
            
            }
            post {
                success {
                    sh "docker version"
                }
                
                }

        }

    }
}
