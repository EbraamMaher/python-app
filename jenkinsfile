pipeline{

    agent {label 'agent'}
    
    stages{
        stage('Build image'){
            steps{
               
                sh "docker ps"
                sh "kubectl get pods -A"
          
            
            
            }
            post {
                success {
                    sh "docker version"
                }
                
                }

        }

    }
}
