pipeline{

    agent {label 'agent'}
    stages{
        stage('build'){
            steps{
                
                echo "Executing pipline"
                kubectl get pods
            }
            post {
                success {
      echo "successfully built"
                }
                
                }

        }

    }
}
