pipeline{

    agent {label 'agent'}
    stages{
        stage('print'){
            steps{
                
                echo "Executing pipline"
            }
            post {
                success {
       slackSend color: "good", message: "successfully built"
                }
                
                }

        }

    }
}
