pipeline{

    agent {label 'agent'}
    stages{
        stage('print'){
            steps{
                
                echo "Executing pipline"
                docker version
            }
            post {
                success {
      echo "successfully built"
                }
                
                }

        }

    }
}
