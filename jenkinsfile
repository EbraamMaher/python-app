pipeline{

    agent {label 'agent'}
    
    stages{
        stage('Build image'){
            steps{
               
                sh "docker ps"
                sh "kubectl version"
          
            
            
            }
            post {
                success {
                    sh "docker version"
                }
                
                }

        }

    }
}
