pipeline{

    agent {label 'agent'}
    
    stages{
        stage('Build image'){
            steps{
               
                sh "docker image ls"
                sh "kubectl get pods -A"
          
            
            
            }
            post {
                success {
                    sh "docker version"
                }
                
                }

        }

    }
}
